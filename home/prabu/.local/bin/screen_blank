#!/bin/sh
while true; do
    # export XDG_RUNTIME_DIR=/run/user/$(id -u)
    # export WAYLAND_DISPLAY=$(ls $XDG_RUNTIME_DIR | grep wayland-.)
    sleep 1200  # 20 minutes
    swaymsg "output * dpms off"
    sleep 20
    swaymsg "output * dpms on"
done
